<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>
<table>
  <tr>
    <th>ユーザー名</th>
    <td><%= @user.name %></td>
  </tr>
</table>
    投稿一覧
<table>
  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= book.user.name %></td>
      <td><%= link_to "show", book_path(book) %></td>
      <td><%= book.created_at.strftime('%Y/%m/%d %H:%M:%S') %>
      <td><%= book.updated_at.strftime('%Y/%m/%d %H:%M:%S') %>
    </tr>
  <% end %>
</table>